<h1>All Restaurants</h1>

<% @restaurants.each do |r| %>
  <h3><%= link_to r.name, restaurant_path(r.id) %></h3>
  <p><%= r.description %></p>
  <% end %>

  <p><%= link_to "Add a Restaurant", new_restaurant_path(@restaurant)%></p>
